import 'package:flutter/material.dart';

class AppColors {
  // ── Backgrounds ──────────────────────────────────────────────────────────
  static const Color bg       = Color(0xFF0B1E2D);
  static const Color card     = Color(0xFF132233);
  static const Color cardAlt  = Color(0xFF0F1E2D);

  // ── Text ─────────────────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFFF7F5F0);
  static const Color textBody    = Color(0xFFB0BEC5);
  static const Color textMuted   = Color(0xFF607D8B);

  // ── Borders / Dividers ───────────────────────────────────────────────────
  static const Color divider = Color(0xFF1A3A4A);

  // ── Fixed accents ─────────────────────────────────────────────────────────
  static const Color gold  = Color(0xFFC9A84C);
  static const Color teal  = Color(0xFF0097A7);
  static const Color green = Color(0xFF43A047);
  static const Color error = Color(0xFFB71C1C);

  // ── Era colours ───────────────────────────────────────────────────────────
  static const Color eraJahiliyyah  = Color(0xFF607D8B);
  static const Color eraEarlyLife   = Color(0xFF00897B);
  static const Color eraMecca       = Color(0xFF00ACC1);
  static const Color eraMediana     = Color(0xFF43A047);
  static const Color eraRashidun    = Color(0xFF43A047);
  static const Color eraUmayyad     = Color(0xFF1E88E5);
  static const Color eraAbbasid     = Color(0xFFC9A84C);
  static const Color eraOttoman     = Color(0xFF7E57C2);
}
